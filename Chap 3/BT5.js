const number = 1;

function sin(number) {
    const result = Math.sin(number);
    console.log(result);
    return result;
};

function cos(number) {
    const result =  Math.cos(number);
    console.log(result);
    return result;
}

function tan(number) {
    const result = Math.tan(number);
    console.log(result);
    return result;
}

sin(number);
cos(number);
tan(number);